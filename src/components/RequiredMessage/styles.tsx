import styled from 'styled-components';

export const ErrorMessage = styled.span`
  font-size: 10px;
  color: #dc3545;
  font-weight: normal;
  position: absolute;
  pointer-events: none;
  left: 5px;
  bottom: -10px;
  background-color: #fff;
`;
